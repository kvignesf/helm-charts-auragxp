apiVersion: networking.istio.io/v1beta1
kind: DestinationRule
metadata:
  name: auragxp-redis-devint2-mtls-disabled
  namespace: {{ .Values.namespace }}
spec:
  host: {{ .Values.istio.redisHost }}
  trafficPolicy:
    tls:
      mode: DISABLE
