{{- if .Values.weeklyBackup.enabled }}
apiVersion: postgresql.cnpg.io/v1
kind: ScheduledBackup
metadata:
  name: {{ .Values.weeklyBackup.name }}
  namespace: {{ .Values.namespace }}
  labels:
    backup-type: full
    backup-tier: weekly
    app: auragxp-cnpg
spec:
  schedule: "{{ .Values.weeklyBackup.schedule }}"
  backupOwnerReference: self
  immediate: {{ .Values.weeklyBackup.immediate }}
  cluster:
    name: {{ .Values.clusterName }}
  target: primary
  method: barmanObjectStore
  backup:
    barmanObjectStore:
      destinationPath: {{ .Values.backup.weeklyDestinationPath }}
      azureCredentials:
        storageAccount:
          name: {{ .Values.backup.secretName }}
          key: CNPG_STORAGE_ACCOUNT
        storageKey:
          name: {{ .Values.backup.secretName }}
          key: CNPG_STORAGE_ACCOUNT_KEY
      data:
        compression: gzip
        immediateCheckpoint: true
{{- end }}
